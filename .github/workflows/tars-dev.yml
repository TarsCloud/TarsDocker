name: tars-dev

on:
  push:
    branches:
      - dev
    paths:
      - "tars/**"
  pull_request:
    branches:
      - dev
    paths:
      - "tars/**"
  schedule:
    - cron: 0 23 * * *
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: buildx
        run: export DOCKER_CLI_EXPERIMENTAL=enabled && docker buildx create --use --name tars-builder && docker buildx inspect tars-builder --bootstrap && docker buildx ls
      - name: test buildx
        run: docker run --rm --privileged docker/binfmt:820fdd95a9972a5308930a2bdfb8573dd4447ad3
      - name: Build tars-dev image
        run: docker buildx build ./tars/ --file ./tars/Dockerfile --tag tars:dev-$(date +%s) --platform=linux/amd64,linux/arm64
